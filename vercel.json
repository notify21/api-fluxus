{
    
    "builds": [{
        "src": "api/main.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "20mb"}
    }],
    "route": [
        {
            "src": "/(.*)",
            "dest": "api/main.py"
        }
    ]
}
